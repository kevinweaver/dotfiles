
#to extract all the things
extract () {
    if [ -f $1 ] ; then
      case $1 in
        *.tar.bz2)   tar xjf $1     ;;
        *.tar.gz)    tar xzf $1     ;;
        *.bz2)       bunzip2 $1     ;;
        *.rar)       unrar e $1     ;;
        *.gz)        gunzip $1      ;;
        *.tar)       tar xf $1      ;;
        *.tbz2)      tar xjf $1     ;;
        *.tgz)       tar xzf $1     ;;
        *.zip)       unzip $1       ;;
        *.Z)         uncompress $1  ;;
        *.7z)        7z x $1        ;;
        *)     echo "'$1' cannot be extracted via extract()" ;;
         esac
     else
         echo "'$1' is not a valid file"
     fi
}

dnslookup(){
  dig $1 ANY +noall +answer
}


# General Aliases
alias scan="xsane"
alias scanner="xsane"
alias mxlookup="nslookup -q=mx"
alias sublime_text="subl"
alias sublime-text="subl"
alias weechat="weechat-curses"
alias tether="easytether connect"
alias color="~/.dotfiles/gnome-terminal-colors/install.sh"

#Website Project Folders
alias heroes="cd ~/workspace/applications/ems-heroes"
alias 100doc="cd ~/workspace/projects/100DaysOfCode/angular/angular2-tour-of-heroes"

#Rails
alias rakeall="rake db:drop && rake db:create && rake db:migrate && rake db:seed && rake db:populate && rake ts:index"
alias rakealll="rake db:drop && rake db:create && rake db:migrate:redo && rake db:seed && rake db:populate && rake ts:index"
alias herokureset="heroku pg:reset DATABASE --confirm ems-heroes && heroku run rake db:migrate:redo && heroku run rake db:seed && heroku run rake db:populate && heroku run rake db:initialize"
alias guard="bundle exec guard"
